import { 
    activate, 
    stopWalking, 
    followPath, 
    create, 
    handleWalkAway, 
    playAnimation, 
    showDebug,
    getData,
    changeIdleAnim } from "./npc";
import { talk } from "./dialog";
import { Dialog, NPCPathType, NPCType } from "./types";

export {
    activate, 
    stopWalking, 
    followPath, 
    create, 
    handleWalkAway, 
    playAnimation, 
    showDebug, 
    talk,
    Dialog,
    getData,
    NPCPathType,
    NPCType,
    changeIdleAnim
}